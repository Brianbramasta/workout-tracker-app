import { Component } from '@angular/core';

/**
 * Halaman login pengguna
 * Menampilkan form login
 */
@Component({
  selector: 'app-login-page',
  standalone: true,
  template: `
    <h1>Login</h1>
    <!-- TODO: Form login di sini -->
  `
})
export class LoginPageComponent {}
