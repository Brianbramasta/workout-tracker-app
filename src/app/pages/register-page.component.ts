import { Component } from '@angular/core';

/**
 * Halaman register pengguna
 * Menampilkan form pendaftaran
 */
@Component({
  selector: 'app-register-page',
  standalone: true,
  template: `
    <h1>Register</h1>
    <!-- TODO: Form register di sini -->
  `
})
export class RegisterPageComponent {}
